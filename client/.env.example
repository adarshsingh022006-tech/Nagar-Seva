# For local development with Vite proxy (defaults to localhost:5000):
# Leave blank or omit.

# For production deployment where backend is hosted on a separate domain (e.g. Render):
# VITE_API_URL=https://nagar-seva-api.onrender.com/api
